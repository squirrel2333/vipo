# 权限

Docker挂载主机目录访问如果出现cannot open directory .: Permission denied

解决办法：在挂载目录后多加一个`--privileged=true`参数即可

如果是CentOS7安全模块会比之前系统版本加强，不安全的会先禁止，所以目录挂载的情况被默认为不安全的行为，

在SELinux里面挂载目录被禁止掉了额，如果要开启，我们一般使用--privileged=true命令，扩大容器的权限解决挂载目录没有权限的问题，也即

使用该参数，container内的root拥有真正的root权限，否则，container内的root只是外部的一个普通用户权限。

# 数据卷

## -v 宿主与容器之间映射添加容器卷

运行一个带有容器卷存储功能的容器实例. 将docker容器内的数据保存进宿主机的磁盘中

`docker run -it --privileged=true -v /宿主机绝对路径目录:/容器内目录 镜像名`

`docker inspect 容器ID`检查Mounts下的信息，表示是否挂载成功

```
[root@localhost ~]# docker run -it --privileged=true -v /tmp/host_data:/tmp/docker_data --name=u1 ubuntu
root@59331695e0b6:/# cd /tmp/docker_data/
root@59331695e0b6:/tmp/docker_data# ls
root@59331695e0b6:/tmp/docker_data# touch dockerin.txt
#ctrl +PQ
[root@localhost ~]# cd /tmp/host_data/
[root@localhost host_data]# ls
dockerin.txt
[root@localhost host_data]# touch hostin.txt
[root@localhost host_data]# docker ps
CONTAINER ID   IMAGE      COMMAND                  CREATED              STATUS              PORTS                                       NAMES
59331695e0b6   ubuntu     "bash"                   About a minute ago   Up About a minute                                               u1
a8fbd460388f   registry   "/entrypoint.sh /etc…"   21 hours ago         Up 21 hours         0.0.0.0:5000->5000/tcp, :::5000->5000/tcp   sad_nash
[root@localhost host_data]# docker exec -it 59331695e0b6 /bin/bash
root@59331695e0b6:/# cd /tmp/docker_data/
root@59331695e0b6:/tmp/docker_data# ls
dockerin.txt  hostin.txt

[root@localhost host_data]# docker inspect  59331695e0b6
[
    {
    ...
        "Mounts": [
            {
                "Type": "bind",
                "Source": "/tmp/host_data",
                "Destination": "/tmp/docker_data",
                "Mode": "",
                "RW": true,
                "Propagation": "rprivate"
            }
        ],
...
]

```
## 作用

将运用与运行的环境打包镜像，run后形成容器实例运行 ，但是我们对数据的要求希望是持久化的

Docker容器产生的数据，如果不备份，那么当容器实例删除后，容器内的数据自然也就没有了。

为了能保存数据在docker中我们使用卷。

## 特点

1. 数据卷可在容器之间共享或重用数据
2. 卷中的更改可以直接**实时生效**
3. **数据卷中的更改不会包含在镜像的更新中**
4. 数据卷的生命周期一直持续到没有容器使用它为止

## 数据共享

1. docker修改，主机同步获得
2. 主机修改，docker同步获得
3. **docker容器stop，主机修改，docker容器重启看数据是否同步。**

## 读写权限

读写权限：`docker run -it --privileged=true -v /宿主机绝对路径目录:/容器内目录:rw 镜像名` 没有:rw时，默认为rw

只读权限：`docker run -it --privileged=true -v /宿主机绝对路径目录:/容器内目录:ro 镜像名` 容器能够读到宿主机的文件写入，容器内不能写文件

# 容器卷的继承

容器u1: `docker run -it --privileged=true -v /mydocker/u:/tmp --name u1 ubuntu`

容器u2 继承u1的卷规则：`docker run -it --privileged=true --volumes-from u1 --name u2 ubuntu`

容器2就与/mydocker/u 拥有了映射关系，即使u1挂掉，u2的卷映射关系仍旧存在

