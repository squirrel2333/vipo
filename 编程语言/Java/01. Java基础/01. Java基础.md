
# 注释

// 单行注释

/ *xxxxxx* / 多行注释

/\*\* xxxxx \*/ 文档注释

tip：IDEA可以改变字体 颜色等

单行注释和文档注释使用较多

代码的注释不是越详细越好。实际上好的代码本身就是注释，我们要尽量规范和美化自己的代码来减少不必要的注释。若编程语言足够有表达力，就不需要注释，尽量通过代码来阐述。

# 标识符

* Java标识符以字母（大写或小写），美元符，或者下划线开始。之后可以是字母、美元符、下划线或任何字符组合。
* 大小写敏感
* 不能使用关键字做标识符

# 关键字

|  |  |  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 分类 | 关键字 |  |  |  |  |  |  |
| 访问控制 | private | protected | public |  |  |  |  |
| 类，方法和变量修饰符 | abstract | class | extends | final | implements | interface | native |
|  | new | static | strictfp | synchronized | transient | volatile | enum |
| 程序控制 | break | continue | return | do | while | if | else |
|  | for | instanceof | switch | case | default | assert |  |
| 错误处理 | try | catch | throw | throws | finally |  |  |
| 包相关 | import | package |  |  |  |  |  |
| 基本类型 | boolean | byte | char | double | float | int | long |
|  | short |  |  |  |  |  |  |
| 变量引用 | super | this | void |  |  |  |  |
| 保留字 | goto | const |  |  |  |  |  |

Tips：所有的关键字都是小写的，在 IDE 中会以特殊颜色显示。

default 这个关键字很特殊，既属于程序控制，也属于类，方法和变量修饰符，还属于访问控制。

* 在程序控制中，当在 switch 中匹配不到任何情况时，可以使用 default 来编写默认匹配的情况。
* 在类，方法和变量修饰符中，从 JDK8 开始引入了默认方法，可以使用 default 关键字来定义一个方法的默认实现。
* 在访问控制中，如果一个方法前没有任何修饰符，则默认会有一个修饰符 default，但是这个修饰符加上了就会报错。
# 访问控制权限

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| 访问控制符 | 本类 | 同包 | 子类 | 任意位置 |
| public | 可以 | 可以 | 可以 | 可以 |
| protected | 可以 | 可以 | 可以 | 不行 |
| 默认 | 可以 | 可以 | 可以（同包子类）/不行 | 不行 |
| private | 可以 | 不行 | 不行 | 不行 |

# 数据类型

tip：IDEA快捷键：ctrl+d 复制单行至下一行

## 基本类型

### 1. 整数类型

|  |  |  |
| --- | --- | --- |
| 类型 | 存储空间 | 范围 |
| byte | 1B字节 = 8bit位 | -128～127 |
| short | 2B = 16bit位 | -215~215-1 (-32768~32767) |
| int | 4B = 32bit位 | -231~231-1 |
| long | 8B = 64bit位 | -263~263-1 |

* long类型需要加l 或 L 作为后缀
* 整型默认为int

可以看到，像 byte、short、int、long能表示的最大正数都减 1 了。这是为什么呢？这是因为在二进制补码表示法中，最高位是用来表示符号的（0 表示正数，1 表示负数），其余位表示数值部分。所以，如果我们要表示最大的正数，我们需要把除了最高位之外的所有位都设为 1。如果我们再加 1，就会导致溢出，变成一个负数。

### 2. 浮点类型

|  |  |  |
| --- | --- | --- |
| 类型 | 存储空间 | 范围 |
| 单精度float | 4B |  |
| 双精度double | 8B |  |

* float num = 50.1F; 要加F或f
* float有限、离散、有舍入误差，不能精确表示0.1 0.01 0.001这样的负十次幂。最好完全避免使用浮点数进行比较判断。这个和计算机保存浮点数的机制有很大关系。我们知道计算机是二进制的，而且计算机在表示一个数字时，宽度是有限的，无限循环的小数存储在计算机时，只能被截断，所以就会导致小数精度发生损失的情况。这也就是解释了为什么浮点数没有办法用二进制精确表示。
* **可以使用**`**BigDecimal**` **数学工具类**
* 默认为double

### 3. 字符类型

char: 2B = 16位

'x' 可以存储一个字母、汉字
**字符串String 不是关键字，是类** 如:"zxzz"
字符本质还是数字 如：`(int)num`强制转换
转义字符 : `'\t'` 制表符   `'\n'` 换行符

### 4. boolean类型

占一位 true or false

## 引用数字类型

1. 类
2. 接口
3. 数组

# 进制

二进制 十进制 八进制0 十六进制0x

十转二：除以2，余数逆序输出

二转十: x\*2^0 + x\*2^1 + ....

```java
//十进制
int a = 10;
//八进制
int b = 010;
//十六进制
int c = 0x10;
//二进制（JDK8）
int d = 0b10;
```
# 编码方式

* ASCII码 英文
+ 'a'...'z' ---> 97 ... 122
+ 'A' ... 'Z' ---> 65 ... 90
+ '0' ---> 48

小写转大写 `(char) (lowerChar - 32)`

* 简体中文

GB2312<GBK<GB18030(容量关系)

* unicode编码统一文字
+ UTF-8
+ UTF-16...

# 类型转换

## 低转高

低------------------------------->高

byte,short,char->int ->long ->float ->double

```java
int a = 100;

//200默认是int，小容量自动转换为大容量
long b = 200;
//小容量转为大容量
int i = 'A';

//不存在类型转换
long c = 300L;

//int最大值
long d = 2147483647;

//报错,加L解决
long e = 2147483648;
```

```java
// 会按照其中最大的类型运算。
int i = 1;
byte b = 1;
double d = 1.0;
double sum = i + b + d;//混合运算升级为double
```

```java
//会按照 int 类型处理。
byte b1 = 1;
byte b2 = 2;
byte b3 = b1 + b2; //报错，b1 + b2自动升级为int

char c1 = '0';
char c2 = 'A';
int i = c1 + c2;//至少需要int来接收
System.out.print(c1 + c2); //113
```
## 高转低

强制转换：（类型）变量名

高->低

```java
public class Demo3 {
    public static void main(String[] args) {

        //内存溢出
        int i =128;
        byte b =(byte)i;

        //损失精度
        System.out.println((int)23.7);//23
        System.out.println((int)-45.89f);//-45

        short a = 5;
        //wrong: short b = a -3;
        short b = (short) a - 3;
    }
}
```

```java
long l1 = 123L;
long l2 = 123;//int类型的123 自动升为 long
long l3 = 123123123123123;//报错，因为超过了int范围。需要加L

float f1 = 12.3;//报错，因为默认是double，不能自动高转低。应该加f,或强转
float f2 = (float)12.3;
```
注意：

1. 不能对布尔值转换
2. 不能把对象转换为不相干类型
3. 高容量转换为低容量，强制转换
4. 转换时存在内存溢出或精度问题

```java
public class Demo6 {
    public static void main(String[] args) {

        int money = 10_0000_0000;//数字可以用下划线分割
        int years=20;
        int total=money*years;//计算溢出
        long total2 = money*years;//输出错误。默认int，转换前已经存在问题
        long total3 = money*((long)years);//正确。先把一个数转换为long

    }
}
```
## string和数据类型拼接

```java
//String 引用类型
public class Dimo6 {
    public static void main(String[] args) {
        int a = 10;
        int b = 20;
        char c = 'a';//97

        //字符串连接符
        //加法运算符两侧，如果有字符串类型，就会把另一个操作数转换为字符串进行连接
        System.out.println(a);
        System.out.println(""+a+b);//1020
        System.out.println(a+b+"");//30 在前侧会相加
        System.out.println(c+a+"hello");//107hello
        System.out.println(c+"hello"+a);//ahello10
        System.out.println("hello"+c+a);//helloa10

    }
}
```
# 变量

* 每个变量都有类型，可以是基本类型也可以是引用类型
* 变量名必须是合法的标识符
* 变量声明是一条完整的语句，因此以分号结束

**成员变量**：类体中声明的变量。（属性）

1. 类变量：static声明的成员变量，也成为**静态变量**。属于类, 类被加载时创建
2. 实例变量：没有使用static修饰的成员变量。属于实例, new操作符创建该类的实例对象时创建，和对象的生存周期相同
3. 会自动以类型的默认值赋值

**局部变量**：方法体内声明的变量。

* 方法被调用时创建，方法退出后撤销。必须被显式初始化
* 局部变量不能被访问控制修饰符及 static 所修饰, 但是，成员变量和局部变量都能被 final 所修饰
* **存放在栈内存中**
* **不会自动赋值**

type varName = value; 可以用逗号隔开声明多个同类型变量

```java
public class Demo7 {
    public static void main(String[] args) {

        int a=1;//换行保证可读性
        int b=2;
        int c=3;
        String name = "xxxx";
        char x = 'x';
        double pi = 3.14;

    }
}
```

```java
public class Demo8 {
    //类变量 static
    static double salary = 2500;

    //属性：变量
    //实例变量（成员变量）: 从属于对象
    String name;
    int age;
    //不进行初始化，会赋默认值
    //数值型、字符型默认值：0 0.0 布尔值:false 除了基本类型，其余默认null

    //main 方法
    public static void main(String[] args) {
        //局部变量：必须声明和初始化值
        int i = 10;
        System.out.println(i);

        //变量类型 变量名字 = new 构造方法名(参数)
        Demo8 deom8 = new Demo8();
        System.out.println(deom8.age);//输出0
        System.out.println(deom8.name);//输出null

        //类变量 static
        System.out.println(salary);
    }
}
```
## 静态变量

***静态变量: JDK6之后, 静态变量从逻辑上的方法区(永久代或元空间)中移到了java.lang.Class对象的末尾, 即挪到了堆中. 但方法区本身也是堆的一个逻辑部分.***

静态变量也就是被 static 关键字修饰的变量。它可以被类的所有实例共享，无论一个类创建了多少个对象，它们都共享同一份静态变量。也就是说，静态变量只会被分配一次内存，即使创建多个对象，这样可以节省内存。

静态变量是通过类名来访问的，例如StaticVariableExample.staticVar（如果被 private关键字修饰就无法这样访问了）。

# 常量

初始化后不能再改变值

final  常量名 = 值

常量名通常使用大写字母

* final要手动复制，没有默认值
* final修饰的实例变量一般添加static修饰
* final修饰的类无法继承
* final修饰的方法无法覆盖
* final变量只能赋值一次
* final修饰的引用一旦指向某个对象，则不能再重新指向其它对象，但对象内部数据可更改
* final变量一般和static联合使用称为常量

```java
public class Demo9 {
    //修饰符不存在先后顺序 static final.... 可换顺序
    static final double PI = 3.14;

    public static void main(String[] args) {
        System.out.println(PI);
    }
}
```
# 命名规范

* 所有变量、方法、类名 **见明知意**
* 类成员变量：首字母小写和驼峰原则：monthSalary 除第一个以外，后面的单词首字母大写
* 局部变量：首字母小写和驼峰原则
* 常量：大写字母和下划线
* 类名：首字母**大写**和驼峰原则 Man GoodMan
* 方法名：首字母小写和驼峰原则

# 运算符

## 算数运算符

+ - \* / % ++ --

```java
public class Demo01 {
    public static void main(String[] args) {
        int a = 10;
        int b = 26;
        int c = 30;
        int d = 20;

        System.out.println(a+b);
        System.out.println(a-b);
        System.out.println(a*b);
        System.out.println(a/(double)b);//强转
    }
}
```

```java
int num = 1234;

int ge = num % 10;
int shi = num / 10 % 10;
int bai = num / 100 % 10;
int qian = num / 1000 % 10;
```

```java
public class Demo02 {
    public static void main(String[] args) {
        long a = 12313123123L;
        int b = 123;
        short c = 10;
        byte d = 8;

        System.out.println(a+b+c+d);//long
        System.out.println(d+c);//int
        System.out.println(c+d);//int
        //多个操作中，有long为long，没有long为int
    }
}
```

```java
public class Demo03 {
    public static void main(String[] args) {
        //++ -- 自增 自减 一元运算符
        int a = 3;

        int b = a++; //先赋值再自增
        int c = ++a; //先自增再赋值
    }
}
```

```java
//幂运算 2^3 2*2*2 = 8 可以使用工具类操作
double pow = Math.pow(2,3); //java.lang
System.out.println(pow);
```
## 关系运算符

* 关系运算符：>, <, >=, <=, ==, !=, **instanceof**

## 赋值运算符

* 赋值运算符: =
* 扩展运算符:  +=,  -= , \*= , /=

```java

        a+=b;//a=a+b
        a-=b;//a=a-b
```
## 逻辑运算符

`a & b`如果a为false，继续执行b

`a && b` 如果a为false，不执行b

`a | b`如果a为true，继续执行b

`a || b`如果a为true，不执行b

`a ^ b`a，b布尔值相同为false，不同为true

```java
//短路运算
int c = 5;
boolean d = (c<4)&&(c++<4);
System.out.println(d);//false
System.out.println(c);//5
```
## 位运算符

* 位运算符: & | ^ ~ >> << >>>

![](Java基础_001.png)

```java
public class Demo05 {
    public static void main(String[] args) {
        /*
        A = 0011 1100 -> 2^2 + 2^3 +2^4 +2^5 = 60
        B = 0000 1101 -> 2^0 + 2^2 + 2^3 = 13

        A&B 0000 1100 与 60|13 = 12
        A|B 0011 1101 或
        A^B 0011 0001 异或 相同为0 否则为1
        ~B  1111 0010 取反

        2*8 = 16 2*2*2*2
        <<  相当于*2
        >>  相当于/2 //与底层相关，效率很高

        0000 0000    0
        0000 0001    1
        0000 0010    2
        0000 0011    3
        0000 0100    4
        0000 1000    8
        0001 0000    16

         */
        System.out.println(2<<3);// 16
    }
}
```
* `<<` :左移运算符，向左移若干位，高位丢弃，低位补零。`x << 1`,相当于 x 乘以 2(不溢出的情况下)。
* `>>` :带符号右移，向右移若干位，高位补符号位，低位丢弃。正数高位补 0,负数高位补 1。`x >> 1`,相当于 x 除以 2。
* `>>>` :无符号右移，忽略符号位，空位都以 0 补齐。

由于 double，float 在二进制中的表现比较特殊，因此不能来进行移位操作。

移位操作符实际上支持的类型只有**int和long**，编译器在对short、byte、char类型进行移位前，都会将其转换为int类型再操作。

**如果移位的位数超过数值所占有的位数会怎样？**

当 int 类型左移/右移位数大于等于 32 位操作时，会先求余（%）后再进行左移/右移操作。也就是说左移/右移 32 位相当于不进行移位操作（32%32=0），左移/右移 42 位相当于左移/右移 10 位（42%32=10）。当 long 类型进行左移/右移操作时，由于 long 对应的二进制是 64 位，因此求余操作的基数也变成了 64。

也就是说：x<<42等同于x<<10，x>>42等同于x>>10，x >>>42等同于x >>> 10。

## 条件运算符

* 条件运算符 : ？ ：

```java
public class Dimo07 {
    public static void main(String[] args) {
        //x ? y : z
        //如果为true，则结果为x，否则为z

        int score = 50;
        String type = score < 60 ? "不及格":"及格";
        System.out.println(type);
    }
}
```
# 包机制

**定义包：**

`package pkg1[.pkg2[.pkg3...]];`

**一般利用公司域名倒置作为包名** com.Huang.xxx

**导入包：**

`import package1[.package2...].(classname|\*);`

`import com.Huang.base.\*(通配符)` 导入包下的所有类

protected 只能在本类、同包、子类中访问

`javac -d . HelloWorld.java //带包编译`

# javaDoc

命令行：javadoc -encoding UTF-8 -charset UTF-8 doc.java

```java
/**
 * @author hqw
 * @version 1.0
 * @since 1.8
 */
//加在类之上就是类的注释，加在方法上就是方法的注释
public class Doc {
    String name;

    /**
     * @auther 作者名
     * @version 版本号
     * @since 指明版本需要最早使用的jdk版本
     * @param name 参数名
     * @return 返回值情况
     * @throws Exception 异常抛出情况
     */
    public String test(String name) throws Exception{
        return name;
    }
}
```
